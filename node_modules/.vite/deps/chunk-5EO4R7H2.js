// node_modules/@mui/utils/contains/contains.mjs
function contains(parent, child) {
  var _a;
  if (!parent || !child) {
    return false;
  }
  if (parent.contains(child)) {
    return true;
  }
  const rootNode = (_a = child.getRootNode) == null ? void 0 : _a.call(child);
  if (rootNode && rootNode instanceof ShadowRoot) {
    let next = child;
    while (next) {
      if (parent === next) {
        return true;
      }
      next = next.parentNode ?? next.host ?? null;
    }
  }
  return false;
}

export {
  contains
};
//# sourceMappingURL=chunk-5EO4R7H2.js.map
